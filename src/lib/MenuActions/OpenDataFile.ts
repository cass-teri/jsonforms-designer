export async function OpenDataFile(
    data_schema_path: string,
    SetDataBuffer: (buffer: string) => void,
    SetIsDataDirty: (is_dirty: boolean) => void
) {
    console.log("open_data_schema")
}
